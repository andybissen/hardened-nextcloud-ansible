# Copy this file to inventory.ini and set your host + SSH details:
#   cp inventory.ini.example inventory.ini
# The real inventory.ini is gitignored so your host isn't published.

[nextcloud_servers]
nc.example.com ansible_user=root ansible_ssh_private_key_file=~/.ssh/id_ed25519
