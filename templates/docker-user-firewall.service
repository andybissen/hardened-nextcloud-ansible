[Unit]
Description=Ensure DOCKER-USER firewall rules (block external DB/cache access)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/docker-user-firewall.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
